[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate --config prisma.config.mjs",
  "npx prisma migrate deploy",
  "npm run build"
]

[start]
cmd = "node dist/server.js"